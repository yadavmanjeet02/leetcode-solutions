// Title: Validate Binary Search Tree
            // Difficulty: Medium
            // Language: C++
            // Link: https://leetcode.com/problems/validate-binary-search-tree/

               check(
                    )
                &&
                    lb
                    root->left,
                    root->val,
        return check(

            return false;
           root->val<=lb)
        if(root->val>=ub ||

            return true;
        if(root==NULL)

               long long lb){

    bool check(TreeNode* root,long long ub,
private:
class Solution {
