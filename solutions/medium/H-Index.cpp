// Title: H-Index
            // Difficulty: Medium
            // Language: C++
            // Link: https://leetcode.com/problems/h-index/


            

        }
        return h;
    }
    int hIndex(vector<int>& citations) {
        sort(citations.begin(),citations.end());
            if(cit[i]>=h) cnt++;
        if(cnt<h) return check(cit,h-1);
        for(int i=0;i<cit.size();i++){
        int cnt=0;
    int check(vector<int>&cit, int h){
public:
class Solution {
